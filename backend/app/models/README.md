# Models

SQLAlchemy models go here.

MVP models:

- `Team`
- `Player`
- `Game`
- `PlayerGameStats`

Later models:

- `User`
- `UploadJob`
- `MetricSnapshot`
- `Report`

Do not start with every model. Build the four MVP models first, then add depth once the upload and dashboard flow works.
