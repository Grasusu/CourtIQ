# Core

Core application setup goes here.

Future files:

- `config.py` for environment settings.
- `database.py` for SQLAlchemy session setup.
- `security.py` for password hashing and JWT helpers.

Keep infrastructure code here so API routes and analytics code stay clean.
